# Session: TICKET-001 — Load Chrome extension in browser

Date: 2026-04-23
Agent: Claude

## Objective

Finish the extension validation loop: confirm the built extension can be loaded in Chrome and the popup lead-capture flow works end to end. From the agent environment I can get the build/API to a known-good state but I cannot click "Load unpacked" in Chrome — that step is handed to the user with precise instructions below.

## Changes Made

- `apps/extension/public/manifest.json`: removed the `icons` block (`icons/icon16.png` / `icon48.png` / `icon128.png`). No icon files exist anywhere in the repo (`find apps/extension -iname "*.png" -o -iname "*.ico" -o -iname "*.svg"` returned nothing), so Chrome was flagging the extension as having missing resources on load. Without the block Chrome uses its default toolbar icon and loads cleanly. If branded icons are wanted later, that's a separate task — just drop PNGs into `apps/extension/public/icons/` and reinstate the manifest block.
- `apps/extension/dist/*`: regenerated by `pnpm --filter extension build`. New build tree: `dist/manifest.json` (no icons block), `dist/popup/{index.html,popup.js}`, `dist/background/service-worker.{js,d.ts,d.ts.map,js.map}`.
- `tickets/backlog.md`: appended a 2026-04-23 progress block to TICKET-001 listing the manifest fix, what was verified on the API contract today, and the exact remaining human-only browser step. Left status `open` until a human confirms the in-browser popup flow.
- This session file.

## Verification

- `git status --short --branch`: clean at start (`main...origin/main`).
- `pnpm --filter extension build` (twice — once before the manifest fix, once after): Vite built `popup/index.html` + `popup/popup.js` (158 kB), tsc emitted the service worker. No errors.
- Extension dist tree confirmed: `manifest.json`, `popup/index.html`, `popup/popup.js`, `background/service-worker.js` all present at the paths referenced from `dist/manifest.json`.
- API running locally from the task-ID `bb0l9jwdq` background job (`pnpm --filter api dev`), health endpoint returned 200.
- CORS: `apps/api/.env` has `CORS_ORIGINS="http://localhost:5173,chrome-extension://*"`; `apps/api/src/app.ts:40-41` splits that list into the `@fastify/cors` `origin` config. Chrome-extension origin is honored.
- Seeded DB: `admin@demo.com` / `password123`, clinic `Westside Physiotherapy`, 3 active events (Vancouver Marathon Expo, TechCorp Employer Benefits Day, Westside Health Fair) each with a default form template version.
- Full API contract smoke test (the exact calls the popup makes):
  - `POST /auth/login` with demo creds → JWT + user payload including clinic membership.
  - `GET /clinics/<clinicId>/events?status=active` → 3 events, each with `defaultFormTemplateVersionId`.
  - `GET /form-template-versions/11111111-0000-0000-0000-000000000001` → 6 fields (`first_name`, `last_name`, `email`, `phone`, `race_distance`, `primary_concern`) with `options` parsed as JSON per the 2026-04-21 fix.
  - `GET /events/44440000-…-0001/consent-requirements` → 2 requirements with the full nested `consentTemplateVersion.consentTemplate.consentType` structure the popup's `ConsentBlock.tsx:16` relies on (I initially thought `consentType` was undefined — that was my curl/node parsing script reading the wrong path, not an API bug).
  - `POST /leads` with the full payload the popup would build → first submission `status=ready`, `duplicateCandidateCount=0`.
  - `POST /leads` with a second lead reusing the first lead's email → `status=needs_review`, `duplicateCandidateCount=1`. This is the exact signal the popup's success screen uses (`App.tsx:214-218`) to render the duplicate warning.
- Not run by me: `pnpm --filter extension typecheck` / `lint`. Build succeeded, which is the contract this ticket actually cares about.
- **Not performed from agent environment**: the literal act of opening `chrome://extensions` and clicking "Load unpacked". See "Human-only remaining step" below.

## Human-only remaining step

1. Confirm the API is running at `http://localhost:3000` (the background job `bb0l9jwdq` from this session is still alive, but if you've killed it since, run `pnpm --filter api dev` from the repo root).
2. In Chrome, open `chrome://extensions`.
3. Toggle **Developer mode** on (top-right).
4. Click **Load unpacked** and pick `C:\Users\paint\eventintake\apps\extension\dist`.
5. The "Event Intake" tile should appear with no red "Errors" button. The toolbar icon will be the default Chrome puzzle piece (intentional — no icons shipped).
6. Click the Event Intake toolbar icon. The popup should render the login screen. Enter `admin@demo.com` / `password123` and click **Sign in**.
7. The popup should move to the event-select screen and list three events. Pick **Vancouver Marathon Expo 2026**.
8. Fill the form (any plausible values), check both the required consent checkbox and optionally the marketing one, click **Submit**. Expect the success screen with a green check and **no** duplicate warning.
9. Click **Capture Another Lead**, fill in a new first/last name but reuse the **same email** as the previous lead, submit. Expect the success screen to show the amber "⚠ Possible duplicate detected — an admin will review before EHR sync." banner.
10. If all six visual checkpoints above pass, TICKET-001 meets acceptance; edit `tickets/backlog.md` to mark it `done` and commit.

If any step breaks, paste the exact error (from the popup, the `chrome://extensions` errors button, or the API logs) and I'll fix it in a targeted session.

## Canonical Updates

- `tickets/backlog.md`: appended today's progress to TICKET-001. Status still `open` pending human confirmation of the in-browser flow.
- `README.md`, `docs/architecture.md`, `docs/implementation-plan.md`, `docs/close-procedure.md`: not changed. The extension build command `pnpm --filter extension build` and the `dist/` path are already the documented shape.

## Deferred Work

- Real branded icons for the extension (`apps/extension/public/icons/icon16.png`, `icon48.png`, `icon128.png` + reinstate the manifest block). Deferred because it's a design/asset task, not a blocker for validation.
- Pre-existing `pnpm --filter api typecheck` failures under `exactOptionalPropertyTypes` — still tracked by scheduled routine `trig_01W3CN9QgExdDXKJ1iaoNeib` (2026-04-30). Unchanged by this session.

## Git State

- Committed: pending at time of writing this note.
- Pushed: pending.
- Commit: TBD after close.
- Deferred reason: N/A — the commit will go out right after this file is saved.
