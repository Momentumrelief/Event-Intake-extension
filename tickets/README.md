# Tickets

Use this folder for deferred bugs, future features, open decisions, and research tasks.

Start with `tickets/backlog.md` for lightweight items. Create a dedicated ticket file when an item needs more detail than a short backlog entry.

Ticket files should use `tickets/TEMPLATE.md`.
